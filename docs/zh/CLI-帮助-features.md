# codex features（中文）

查看或切换功能开关。

## 用法
```
codex features list
codex features enable <FEATURE>
codex features disable <FEATURE>
```

## 说明
- `list` 显示已知功能与当前状态。
- `enable/disable` 会写入 `config.toml`。
