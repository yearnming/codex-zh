# Codex CLI 中文化项目方案与计划

## 目标
- 将 Codex CLI 的界面、提示、帮助信息与文档进行中文化。
- 与官方仓库保持同步：官方更新后，中文化版本能够自动跟进并发布。
- 将中文化成果发布到 GitHub，形成可持续维护的开源项目。

## 范围
- CLI 运行时输出、交互提示、错误信息、帮助文档（`--help`）。
- 项目仓库文档（README、使用指南、贡献指南）。
- 不改变核心功能逻辑，不引入与官方不一致的行为。

## 原则
- **最小改动**：尽量把中文化改动集中在可维护的层（文案层、资源层）。
- **可回放**：所有中文化改动可用补丁或脚本在新版本上重放。
- **可审计**：变更路径清晰，能快速定位与官方差异。

## 上游信息
- 上游仓库：`https://github.com/openai/codex`
- 许可证：Apache-2.0（以 upstream LICENSE 为准）

## 技术方案（建议）

### 方案 A：官方仓库 Fork + 中文化补丁层（推荐）
- 保持 `upstream` 指向官方仓库，`main` 跟随官方。
- 新建 `l10n/zh` 分支，仅包含中文化补丁。
- 每次同步流程：`git fetch upstream` → `git rebase upstream/main` → 自动重放中文化补丁。
- 中文化内容尽量集中在独立文件（如 `locales/zh.json`）或明确的字符串映射层。

### 方案 B：上游镜像 + 补丁仓库
- 镜像仓库只做上游同步；中文化单独维护在补丁仓库中。
- CI 在镜像更新后，自动将补丁应用并生成发布产物。

### 自动化与 CI
- GitHub Actions 定时（如每天）拉取上游更新。
- 若上游有新提交：
  - 自动尝试重放中文化补丁。
  - 运行基本测试（或 smoke test）。
  - 生成变更报告并自动开 PR。
- 生成中文化覆盖率报告（新增/修改字符串提醒）。

### 发布策略
- 发布版本号与官方保持一致，必要时追加 `-zh` 后缀。
- Release Notes：列出上游更新摘要与中文化变更。

## 里程碑与任务状态

状态定义：`待办` / `进行中` / `已完成` / `阻塞`

| 编号 | 任务 | 负责人 | 状态 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | 创建项目方案与计划文档 | 我 | 已完成 | 本文档 |
| 2 | 明确上游仓库地址与许可证 | 待定 | 已完成 | 已确认上游地址与许可证 |
| 3 | 选定同步策略（A 或 B） | 待定 | 已完成 | 采用方案 A |
| 4 | 初始化 GitHub 仓库与分支结构 | 待定 | 进行中 | 已配置 origin/upstream，`l10n/zh` 已推送，待 `main` 对齐 upstream 并设置默认分支 |
| 5 | 设计中文化资源结构（如 `locales/zh.json`） | 待定 | 待办 | 目标是最小侵入 |
| 6 | 建立上游同步脚本 | 待定 | 已完成 | `scripts/sync_upstream.sh` |
| 7 | 配置 GitHub Actions 自动同步 | 待定 | 已完成 | `.github/workflows/sync-upstream.yml` |
| 8 | 初版中文化覆盖（核心交互与帮助） | 待定 | 待办 | |
| 9 | 发布 v1 中文化版本 | 待定 | 待办 | |
| 10 | 建立贡献流程与翻译规范 | 待定 | 待办 | `CONTRIBUTING.md` |

## 推进说明（下一步）
- 先确认官方仓库与许可（任务 2）。
- 选定同步策略（任务 3），再开始仓库与分支初始化（任务 4）。
- 在此基础上启动中文化资源结构与 CI 自动化。
